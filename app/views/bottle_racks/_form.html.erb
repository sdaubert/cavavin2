<%= form_with(model: bottle_rack, local: true) do |form| %>
  <% if bottle_rack.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bottle_rack.errors.count, "error") %> prohibited this bottle_rack from being saved:</h2>

      <ul>
      <% bottle_rack.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :lines %>
    <%= form.number_field :lines %>
  </div>

  <div class="field">
    <%= form.label :columns %>
    <%= form.number_field :columns %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
